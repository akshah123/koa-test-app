version: "3.6"
services:
  app:
    command: yarn
    entrypoint: ""
    depends_on: []
    volumes:
      - ../package.json:/app/package.json
      - ../yarn.lock:/app/yarn.lock
      - ../../../../modules:/app/modules

